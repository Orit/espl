 

PROGNAME=invert

all: start

start:    invert.o
	gcc -g invert.o -ansi -m32 -Wall -o $(PROGNAME)

invert.o:    invert.c
	gcc -g -Wall -c invert.c -o invert.o

.PHONY : clean

clean : 
	rm $(PROGNAME) invert.o

memcheck:
	valgrind --leak-check=yes --track-origins=yes ./$(PROGNAME)
